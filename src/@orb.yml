version: 2.1

description: >
  An integrated package of parameterizable and reusable CI/CD
  pipeline configuration elements that are used to build and
  deploy a single-instance service.

display:
  home_url: "https://github.com/relaxdiego/service-pipeline-orb"
  source_url: "https://github.com/relaxdiego/service-pipeline-orb"

# If your orb requires other orbs, you can import them like this.
# Otherwise remove the "orbs" stanza.
# orbs:
#  hello: circleci/hello-build@0.0.5
